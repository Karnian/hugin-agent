Goal: Implement protocol v1.6-draft following docs/v1.6-work-brief.md as the authoritative spec (READ IT FIRST — §5 lists every exact edit: files, symbols, regex, vectors, fields). Mechanical fixes from 3 reviews; no redesign.

WORKING METHOD (follow strictly):
1) PLAN: write a parallel implementation plan (mark which edits are independent vs dependent); have Codex review the plan; iterate until Codex finds no problems.
2) EXECUTE in parallel once the plan is clean. After each unit of work, have Codex review the diff; loop fix->re-review until Codex passes it.
3) PHASE GATES: at every phase boundary run `npm run typecheck` + `npm run protocol:check` AND a Codex cross-review; advance only when all are clean.
Use the agent-olympus:ask agent (routes to Codex) for every review. Always loop review->fix->review until zero issues. Work to completion this way; be exhaustive.

Suggested phasing (refine in your plan; full detail in brief §5):
- P1 (parallel): 5a messages.ts schema fixes (nonce->length(43); add AuthId /^[A-Za-z0-9._-]{1,128}$/ on challenge_id/agent_id/key_id; dedupe SemVer into messages.ts +.max(64), import in index.ts, delete index.ts's copy; remove dead SIG_MAX; PROTOCOL_VERSION="1.6.0-draft") || 5c auth-pairing-spec.md edits (F2 server_origin reject-non-canonical/DNS-only/reject IP+port0; F3 tenant_id 1*128 + update §2/§11 from <=64 to <=128 + stability/no-rebind; F5 linearizable nonce-CAS/per-agent_id-monotonic-epoch/lease-CAS; identity tenant>user>agent(device)>engines, agent_id per-device, user_id off-wire; clarifications). Gate: typecheck + Codex review.
- P2: 5d cross-language test vectors — create protocol/v1/gen-vectors.ts -> protocol/v1/test-vectors.json (Ed25519 from a FIXED 32-byte seed; transcript per auth-spec §5: LP(x)=uint32_be(len)||x with nonce_raw as raw 32 bytes NO LP; fields incl domain_tag_hex, alg_lp_hex, transcript_hex, expected_signature_base64url; positive 43-char nonce; negatives MUST fail: 42/44-char & padded nonce, uppercase host, :443, trailing dot, IDN host, 129-char tenant_id, non-ASCII & invalid agent_id, >64 & differing protocol_version) + load/verify in selftest.ts via its checks array. Gate: protocol:check + Codex review.
- P3: docs — 5b README new "## Event kinds" rule (unknown core->NACK; vendor passthrough; sender emits only <=negotiated core) + 5e CHANGELOG [1.6.0-draft] and README rev1.6 / move tenant_id+server_origin to Resolved / delete "event.kind membership deferred" + 5f rewrite PROPOSAL.md for v1.6 (F1-F6 + Codex A/B/C closed; locked decisions stated; only post-freeze operational confirms remain). Gate: Codex review.

Invariants (brief §4, do NOT relitigate): EventKind core FROZEN for v1 (vendor.<engine>.* only); agent_id per-device with a user 1:N layer (user_id off-wire).

Done when: `npm run typecheck` clean; `npm run protocol:check` passes 23/23 + all edge checks + new vector checks (positives sign-match, negatives fail) + reject-44-char-nonce + reject-non-ASCII-agent_id + reject->64-char-SemVer; PROTOCOL_VERSION=="1.6.0-draft"; protocol/v1/test-vectors.json exists and is verified by selftest; no doc says event.kind membership is deferred; PROPOSAL.md targets v1.6->v1.0.0; AND a final Codex cross-review reports zero remaining issues.
