Goal: Implement protocol v1.6-draft in this repo, following docs/v1.6-work-brief.md as the authoritative spec (READ IT FIRST — §5 has the exact files/symbols/regex/vectors). These are mechanical fixes from 3 reviews; no redesign.

Edits (full detail in brief §5):
- messages.ts: nonce -> Base64Url.length(43); add AuthId=/^[A-Za-z0-9._-]{1,128}$/ and apply to challenge_id/agent_id/key_id; dedupe SemVer (export from messages.ts, import in index.ts, delete index.ts's own regex) + .max(64); remove dead SIG_MAX; PROTOCOL_VERSION="1.6.0-draft".
- README: add "## Event kinds" subsection (unknown core->NACK, vendor passthrough, sender emits only <=negotiated core); move tenant_id/server_origin to Resolved; delete the "event.kind membership deferred" line.
- auth-pairing-spec.md: F2 server_origin (reject non-canonical input; DNS-only in prod, reject bare IP/port-0/zone-id; omit default port; lowercase+IDNA). F3 tenant_id grammar 1*128 and update §2 table + §11 item 1 from <=64 to <=128, add lifetime stability + no silent cross-tenant rebind. F5 concrete linearizability: nonce CAS, per-agent_id strictly-monotonic connection_epoch, lease ownership CAS/fencing. Identity model tenant>user>agent(device)>engines; resolve agent_id to per-device; user_id off-wire (pairing record + pairing response + audit only). Clarify: base64url unpadded; server_origin/tenant_id reconstructed by verifier, not on wire; transcript protocol_version is the exact hello value; challenge_ttl_ms advisory. Add strawman values for rotation-grace / pairing-code-TTL / audit-schema, marked post-freeze (not blockers).
- NEW protocol/v1/gen-vectors.ts -> protocol/v1/test-vectors.json. Ed25519 from a FIXED 32-byte seed (reproducible). Transcript per auth-spec §5: LP(x)=uint32_be(len)||x, with nonce_raw inserted as the raw 32 bytes (NO LP). Fields per vector incl: domain_tag_hex, alg_lp_hex (=LP("ed25519")), transcript_hex, ed25519_seed_hex, ed25519_public_hex, expected_signature_base64url. Positives: a valid 43-char nonce. Negatives (must fail, with reason): 42- and 44-char nonce, padded nonce, uppercase host, :443 present, trailing-dot host, IDN host, 129-char tenant_id, non-ASCII agent_id, invalid-charset agent_id (e.g. agent@host), >64-char protocol_version, differing protocol_version. selftest.ts loads test-vectors.json and verifies positives (recompute transcript + signature match) and negatives (documented failure) via its `checks` array.
- PROPOSAL.md: rewrite for v1.6 — mark F1-F6 + Codex A/B/C closed; state locked decisions (EventKind frozen for v1, agent_id per-device + user layer); list ONLY post-freeze operational confirms remaining; ask for a diff-review then v1.0.0. Must not still say "event.kind membership deferred".
- CHANGELOG.md: add a [1.6.0-draft] section.

Done when: `npm run typecheck` is clean AND `npm run protocol:check` passes 23/23 + all existing edge checks + the new vector checks (positives sign-match, negatives fail) + reject-44-char-nonce + reject-non-ASCII-agent_id + reject->64-char-SemVer; PROTOCOL_VERSION=="1.6.0-draft"; protocol/v1/test-vectors.json exists and is verified by selftest; no doc says event.kind membership is deferred; PROPOSAL.md targets v1.6 -> v1.0.0.
